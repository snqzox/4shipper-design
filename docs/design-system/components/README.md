# Components

One Markdown file per design-system component lives here, e.g. `button.md`, `text-field.md`.
Generated and maintained by the **design-system-manager** agent (descriptions written by the
**ds-component-describer** subagent). The component inventory itself comes from `data/components.json`.
