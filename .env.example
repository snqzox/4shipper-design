# Figma Personal Access Token (read-only).
# Create one at: https://www.figma.com/settings  →  "Personal access tokens"
# Required scopes: "File content" (read) and "Library content" (read).
FIGMA_TOKEN=

# Figma file keys. Defaults live in scripts/config.mjs — only set these to override.
FILE_KEY_UIKIT=XVou4XJ4rWbt4oXoSxO7hO
FILE_KEY_DESIGN=Xol48qmGXL8hIqA42jbHno
